# No .env.local needed here — next.config.ts loads the project-root .env automatically.
# Fill in the root .env (see ../.env.example) with these three vars:
#   SUPABASE_URL, SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY
#
# For Vercel deployment, set those same three vars in the Vercel project dashboard.
